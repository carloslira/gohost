const sidebarWidth = 256;

const transition = {
    transition: 'all .3s ease'
};

const defaultFont = {
    fontFamily: '"Pluto", "Helvetica", "Arial", sans-serif'
};

const primaryColor = '#F7A600';
const secondaryColor = '#01305C';

const grayColor = '#ABABAB';
const darkGrayColor = '#4C4C4C';
const lightGrayColor = '#E1E2E1';

export {
    grayColor,
    transition,
    defaultFont,
    sidebarWidth,
    primaryColor,
    darkGrayColor,
    lightGrayColor,
    secondaryColor
};